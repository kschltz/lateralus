(ns kschltz.lateralus-test
  (:require [clojure.test :refer [deftest is testing]]
            [kschltz.lateralus :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 0))))
